# M2.5 — SDK extraction (Phase 1 unblock)

**Status:** 🟢 RUNTIME GREEN (publish/live-trading gated — see `sdk-m1-scaffold/STATUS.md`)
**Theme:** Extract `ChartRunnerSDK` from the inline IIFE in `ChartRunner_Prototype.html` into the publishable `@chartrunner/core` npm package. Unlocks Phase 1 — running ChartRunner abilities as overlays on top of Dexscreener / TradingView host charts.

> **Reconciled 2026-05-20** (`MILESTONE_AUDIT.md §3`). `sdk-m1-scaffold/` already had the SDK surface catalogued, the order-issuing broker (`SandboxBroker.ts`) + detectors ported to TS, interface stubs (`ChartHost`/`AbilityRegistry`/`BrokerAdapter`/`RiskManager`/`SignalFeed`), and a working build pipeline. At that time **M1.4** (inline swap) and **M1.5** (static regression) were still pending. Note: the scaffold's internal "M1.x" numbering = SDK sub-phases, *not* roadmap-M1 (Tokenomics).

> **Updated 2026-05-30:** M1.4/M1.5 are now green. The prototype consumes the generated `ChartRunnerCore` IIFE between `BEGIN_CHART_RUNNER_CORE_BUNDLE` / `END_CHART_RUNNER_CORE_BUNDLE`; the old inline `class ChartRunnerSDK` is gone. `AbilityRegistry.registerCoreAbilities()` exports the full ability palette, with trade initiation bound to hotkey `4`. `npm test` passes **424 checks across 11 suites**. A first Solana Dexscreener paper overlay demo exists at `sdk-m1-scaffold/demos/dexscreener-overlay.html`.

## Completion condition (all required)

- [ ] `@chartrunner/core` package published to npm (v0.1.0) — **GATED:** live publish not run; local guard blocks `npm publish --dry-run` until explicit approval.
- [x] `ChartHost` interface formalized + documented
- [x] `AbilityRegistry` exports all 6 core abilities (bracket, ladder, OCO, hedge, radar, rescue) + the 11 Tier-1-4 primitives
- [x] `ChartRunner_Prototype.html` imports from `@chartrunner/core` via inline-bundler pattern (no behavior change in prototype)
- [x] First external host demo: ChartRunner overlay on a Dexscreener iframe (proof of concept)

## Imminent-solvables

### Ready bucket

> **Dev-kit infra (2026-05-28):** the **P0 bridge-rewiring patch** (staged in [`_patches/p0-bridge-rewiring-2026-05-28/`](../../_patches/p0-bridge-rewiring-2026-05-28/)) extends `dev-panel.html`'s `createRemoteBridge()` to mirror the full bridge.js surface. The SDK-extraction rounds below will need the dev panel to be able to drive `run.startHeadless`, `scoring.getActiveScore`, `terminal.compose.{execute,simulate}` from outside the game tab — all of which were unreachable before this patch. **Not a Ready-bucket item** (no acceptance criterion), but apply it before round 1 so the integration tests have somewhere to call from.

> **All 4 Ready-bucket items done 2026-05-20** (frontier auto-resolve batch). These unblock the Blocked-bucket module-extraction rounds, which are not yet started.

- [x] 2026-05-20 — `[O]` SDK surface inventory — `docs/architecture/M25-sdk-surface.md`. Inventoried **39 public methods + 24 events** with real line citations. Corrected the stale STATUS.md location: the `ChartRunnerSDK` class is at **L10453** (the old 8438–9948 range drifted to Workbench markup). Documented 8 host globals the SDK reaches for.
- [x] 2026-05-20 — `[D]` Bundler pattern decision — `docs/architecture/M25-bundler-decision.md`. Recommendation: **esbuild inline-bundler** — `format:'iife'` is the only one-line config yielding a synchronous, module-free, offline-safe single-file blob for `play/` while the same source emits the npm ESM package.
- [x] 2026-05-20 — `[D]` `@chartrunner/core` package skeleton — `sdk-m1-scaffold/sdk/core/` (`package.json` 0.1.0 ESM+IIFE, `tsconfig.json`, 9 `src/` modules incl. the 5 interfaces; 39 methods stubbed with TODO + line refs, no logic ported).
- [x] 2026-05-20 — `[O]` Prior-attempts audit — `docs/architecture/M25-prior-attempts.md`. Two attempts: the dead runtime module-swap (23k+ lines of sync JS can't become async-module clients) and the live inline-bundler plan; 6 don't-repeat lessons.

### Blocked / gated bucket

- [!] `[D]` npm publish dry run (`npm publish --dry-run`) — **GATED:** local publish guard requires explicit user approval. Non-publishing `npm pack --dry-run --json` is green.
- [!] `[D]` npm publish live (v0.1.0) — **GATED:** user previously said deploy/publish is last; requires explicit approval after dry run.
- [ ] `[D]` Live Solana DEX adapter — needs Jupiter V2 route adapter, wallet approval UX, transaction failure states, and risk guard.
- [ ] `[O]` Venue/on-chain OI feed audit — needed before `detectOIConfirm` leaves pending-stub status.

### Done bucket

- [x] 2026-05-30 — `[D]` Module extraction rounds 1-3 complete: primitives, events, lifecycle, detectors, scoring, `tick`, and `SandboxBroker`.
- [x] 2026-05-30 — `[D]` AbilityRegistry export wiring: `registerCoreAbilities()` delegates through `ChartRunnerSDK`; bracket/trade initiator bound to hotkey `4`.
- [x] 2026-05-30 — `[D]` Inline-bundler integration into prototype: generated IIFE spliced into `ChartRunner_Prototype.html`; no duplicate inline SDK class.
- [x] 2026-05-30 — `[O]` Static regression: `npm test` passes 424 checks across 11 suites; prototype script parse reports 3 scripts / 0 failures.
- [x] 2026-05-30 — `[O]` Package contents dry run: `npm pack --dry-run --json` green, 52 entries, 96.5 KB tarball.
- [x] 2026-05-30 — `[D]` Dexscreener overlay demo: local IIFE + Solana Dexscreener iframe + public pair stats + paper bracket through SDK hotkey `4`.

## State

- Progress: runtime extraction complete; npm publication and live Solana DEX settlement remain gated.
- Blockers active: 4
- Scheduled today: 0

## Notes

- Hard rule from skill: `ChartRunnerSDK` is the only thing that issues orders. That property is what makes extraction cheap — abilities already don't touch the network or canvas.
- M2.5 unblocks M5 (Hyperliquid adapter slots in as another DEX driver), M9 (Solana Mobile RN imports the same package).
- Phase 1 was the original architectural goal — finishing it post-Frontier closes the loop on the v0.5 promise.

### Ecosystem scan 2026-05-14
- **Pyth** `pyth-solana-receiver-sdk` now supports Anchor 0.28.0 → 0.31.1. When `@chartrunner/core` declares an Anchor peer/range, anchor 0.30.1 or 0.31.1 keeps Pyth integration future-proof. Source: `docs/SOLANA-ECOSYSTEM-DAILY.md#2026-05-14`.
