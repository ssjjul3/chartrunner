# 2026-05-14 — Daytime evaluator plan

**Block:** daytime (interactive work, user online)
**Evaluator:** `cr-evaluator-morning`
**Mix:** 1 milestone + 1 marketing (default; first evaluator run, no prior track record)

## Yesterday recap

- No prior `_evaluator-log/` entries — this is the first evaluator run, so there is no overnight log to consume.
- Per memory + `CHANGELOG-v0.9.x.md`, recent ships: v1.0.93–v1.0.101 polish (crSafeHTML, Terminal observer debounce, **Phantom direct-sign repair v1.0.47/99**, whitelist form trim, renderWidgets() destruction removed).
- Git activity: not directly inspectable from the sandbox — `/Users/julianroy/projects/chartrunner` is not mounted. Runtime tasks must read it on the user's machine.

## Today's plan (1 milestone + 1 marketing)

### Pick 1 — [D] M0.5 · Solana Playground oracle deploy attempt @ **10:00 +02:00**

Why this leverage: `chartrunner_oracle` has Pyth-only deps (per the milestone file), so it sidesteps the `block-buffer 0.12` / Rust 1.85 wall that's blocking `chartrunner_match`. Success directly closes a completion-condition checkbox (`chartrunner_oracle deployed to devnet`); failure surfaces a new failure mode that informs path (c) — the vendored block-buffer fork decision. Either outcome is high-information. Active milestone, user-recent area (per memory `project_chartrunner_anchor_deploys.md`). Score ~4.

Artifact: `docs/architecture/M05-oracle-playground-deploy.md`.

### Pick 2 — [D] MKT-X · Draft today's build-in-public X post @ **14:00 +02:00**

Why this leverage: cadence baseline (1/day per `CADENCE.md`); post-Frontier momentum is the active marketing focus this week; pairs with the 10:00 task — fresh deploy news if oracle ships, otherwise the v1.0.99/47 Phantom direct-sign repair is solid fallback material. Score ~3.

Artifact: `docs/marketing/_drafts/2026-05-14-x-build-in-public.md`.

## ⚠ Blocker: scheduling write blocked

`mcp__scheduled-tasks__create_scheduled_task` **failed**: "Cannot create scheduled tasks from within a scheduled task session." Both picks are documented here and tagged `[SCHEDULED 2026-05-14]` in their source files, but the user must either:

1. In a fresh (non-scheduled) Cowork session, ask Claude to create the two scheduled tasks per the prompts below, OR
2. Run the tasks ad-hoc today by pasting the prompts into Cowork chat.

This is a tooling limitation, not a planning issue.

### Self-contained prompt for Pick 1 (10:00)

> ChartRunner M0.5 daytime [D] task: attempt to deploy `chartrunner_oracle` (program ID `7FJjBq98caT4vUc6nMHQHm6ojsURBq9zDVXbrT6VB1BL`) via Solana Playground (beta.solpg.io) to sidestep the local `cargo-build-sbf` / Rust 1.84.1 blocker. Source lives at `/Users/julianroy/projects/chartrunner/anchor/programs/chartrunner-oracle/src/lib.rs`. Use existing oracle keypair from `~/.chartrunner-keypairs/`. Devnet only. Capture build output, deploy tx + Solana Explorer link (success) OR exact error class compared against `docs/ANCHOR-BLOCKER-WATCH.md` known modes (failure). Save artifact to `docs/architecture/M05-oracle-playground-deploy.md` (<600 words). If deploy succeeds, Edit `docs/milestones/M0.5-security.md` to mark `[x] 2026-05-14` and bump State block to 3/15 done. Do NOT touch mainnet, do NOT change upgrade authority.

### Self-contained prompt for Pick 2 (14:00)

> ChartRunner MKT-X daytime [D] task: draft today's build-in-public X post for @ChartRunner_xyz. (1) Check `docs/architecture/M05-oracle-playground-deploy.md` — if 10:00 task shipped a deploy, lead with that. (2) Else, fall back to v1.0.99/47 Phantom direct-sign repair (memory: `project_chartrunner_v1_0_x_polish.md`). (3) Else, version-bump recap from `docs/CHANGELOG-v0.9.x.md`. Constraints: ≤280 chars, builder-confident voice (see `docs/marketing/CADENCE.md` — lead with verb, numbers > adjectives, ≤2 emojis), hit at least one quality gate (build-in-public / educational / ecosystem / vision). Save draft (with screenshot suggestion + 1 alt variant) to `docs/marketing/_drafts/2026-05-14-x-build-in-public.md`. Do NOT post.

## Milestone state

- **M0.5** (active): 2/15 done · 2 blockers (Anchor unblock, audit complete) · 1 scheduled today
- M1–M10: queued, untouched

## Marketing state

- Drafts ready to post: **0** (`_drafts/` empty)
- Pending engagements: **0** (no mention sweep yet — overnight evaluator hasn't run)
- Chrome MCP **degraded** today per `SURFACE-HEALTH-DAILY.md`; engagement tasks deferred until Chrome reconnects.

## Sensors at a glance

- ⚠ SURFACE-HEALTH: Chrome unavailable, deploy drift cannot be verified.
- 🔴 ANCHOR-BLOCKER-WATCH: still blocked (platform-tools v1.54 = Rust 1.84.1).
- ❓ BUGS-CURRENT.md: missing — overnight evaluator should create.
- ❓ SOLANA-ECOSYSTEM-DAILY.md: missing — overnight evaluator should create.
- ❓ marketing-intel: no draft today.
