# M0.5 — Security + Anchor unblock

**Status:** 🟢 ACTIVE (started 2026-05-13)
**Theme:** Wrap upgrade authorities in Squads multisig + book independent Anchor audit + unblock the two stalled program deploys.

> **Update 2026-05-26** ([CONSOLIDATED_STATUS_2026-05-26.md](../../CONSOLIDATED_STATUS_2026-05-26.md)): the registry **SOURCE-AHEAD** pile grew — this session added **name-register (Item 6) + resale/royalty (Item 5) + `record_run` oracle-cite (Item 1)** on top of the v0.9.8 parity gap (`M05-deploy-parity.md`). **DONE 2026-05-27:** one rebuild + 2-of-3 re-upgrade carried **everything** (parity + 6 + 5 + 1), built locally (platform-tools v1.52 + Anchor 0.30.1 cleared the old wall) → write-buffer → set-buffer-authority vault → `run-upgrade.mjs` (exec tx `3XHRv5j…`) → **byte-verified** (deployed == source, sha256 `46c715da…`). **→ registry deploy-parity is CLOSED.** Oracle remains SOURCE-AHEAD (Item 4 SDK upgrade skipped — see `M05-deploy-parity.md`), so the auditor should review registry live bytecode (now current) + oracle source. Runbook: `docs/architecture/registry-upgrade-vX-deploy.md`. (IDL: the committed `anchor/idl/chartrunner_registry.json` was hand-regenerated to current 2026-05-27 — the auditor has an accurate IDL; a toolchain regen still awaits the proc-macro2 `source_file` fix but isn't blocking.)

## Completion condition (all required)

- [x] 2026-05-19 — Squads multisig wraps program upgrade authorities — DONE for **all three** live programs (maps + registry + oracle), exceeding the original two. Vault `fK1J2TLk2qLy3cjtiSYDSuCnWuxezphBcdqNGZEpVsp`. First real 2-of-3 upgrade cycle also executed (oracle v0.9.12).
- [ ] Independent Anchor audit booked + complete (1 firm of OtterSec / Halborn / Neodyme / Zellic) ← **THE one open M0.5 condition**; Neodyme primary, package ready, not yet engaged (your call)
- [x] 2026-05-20 — `chartrunner_match` deployed to devnet — **DONE.** The 13-day Anchor wall fell: built **locally** (not Playground) once Anza shipped **platform-tools v1.52 (rustc 1.89.0)**, clearing the `block-buffer`/edition2024 ≥Rust-1.85 gate (with ERS 0.14.2 + anchor-lang 1.0.2). Hardened to match v0.9.10 (host-only finish) via a Squads upgrade. Source: `docs/WEEKLY-2026-W21.md`.
- [x] 2026-05-19 — `chartrunner_oracle` deployed to devnet (`4vfZVDfDzhR79qdaUdPAzRwUHYB5qbgNwTGBwfy6i5wH`) via Playground no-SDK rewrite; seed bug found + fixed + upgraded through the multisig.

## Imminent-solvables

### Ready bucket (evaluator can pick)

- [x] 2026-05-20 — `[D]` Squads multisig integration walkthrough — **SUPERSEDED by doing it.** The 2-of-3 multisig was set up and a full live upgrade cycle executed (write-buffer → set-buffer-authority → propose → 2 approvals → execute) on all three programs; the signer pattern, transfer flow, and 2-of-3 config are captured in `_security/squads/` (reusable scripts) + `SECURITY_AUDIT_2026-05-19.md`. A separate research walkthrough doc was unnecessary.
- [x] 2026-05-20 — `[D]` Audit firm shortlist + outreach drafts — DONE. `docs/architecture/M05-audit-firms.md` (810 words): OtterSec/Halborn/Neodyme/Zellic compared on 6 dims. **Primary: Neodyme** (Solana-native boutique, best fit for 3 small programs); **backup: Halborn**. Cost lever found: **Areta $1M Solana Audit Subsidy** (~30% off via Halborn/Zellic; Colosseum teams fast-tracked). All pricing is request-quote (no public ranges). Outreach template + pre-send checklist included.
- [x] 2026-05-14 — `[D]` Solana Playground oracle deploy attempt — *path closed at the time* (Playground lacked `pyth-solana-receiver-sdk`). **REOPENED + SUCCEEDED 2026-05-19:** rewrote the oracle without the SDK using local mirror structs (`lib.playground.rs`), which Playground's allowlist accepts. Deployed live. See memory `project_chartrunner_anchor_deploys.md`.
- [x] 2026-05-20 — `[D]` Local `anchor build -p chartrunner_oracle` attempt — **SUPERSEDED.** The oracle is already deployed to devnet via the Playground no-SDK mirror-struct build (`4vfZ…i5wH`) and upgraded through the multisig, so a local-build path for the oracle is moot. The local-toolchain blocker now only matters for `chartrunner_match` (tracked in the Blocked bucket). The "does oracle differ from match" question is answered: yes — oracle shipped, match still 🔴.
- [x] 2026-05-20 — `[O]` Audit prep — DONE. Smart-contract self-audit of all 3 live programs (incl. the deployed oracle .playground variant) in `docs/architecture/M05-self-audit.md`. Found 3 🔴 + several 🟡 (oracle price_update has no owner-check → forgeable; registry list_entity front-run; record_run i32::MIN sharpe wrap). Hand to the firm to cut scope; fixes tracked in task "Fix self-audit findings before mainnet".
- [x] 2026-05-19 — `[O]` Dry-run Squads upgrade-authority transfer — **OBSOLETE / superseded by the real thing.** Instead of a throwaway rehearsal we ran the full live cycle: write-buffer → set-buffer-authority to vault → vaultTransactionCreate → 2 approvals → execute, on the actual oracle program. Reusable scripts saved in `_security/squads/`.
- [x] 2026-05-20 — `[O]` Drift sweep on Anchor IDL files — DONE + **gaps fixed**. `docs/architecture/M05-idl-drift-sweep.md`: registry IDL was in sync; the real gaps (missing oracle/maps IDLs + an oracle program-ID mismatch) are now **resolved** — see the two Done-bucket entries below.

### Blocked bucket (evaluator skips until condition met)

- [x] 2026-05-20 — `[D]` `chartrunner_match` deploy — **DONE (not via Playground).** Anza platform-tools **v1.52 (rustc 1.89.0)** cleared the Rust-1.85 wall, so match built + deployed **locally** with ERS 0.14.2 + anchor-lang 1.0.2, then hardened to v0.9.10 via Squads. The `cr-anchor-blocker-watch` sensor has re-confirmed 🟢 UNBLOCKED 4 days running — **disable it** (needs an interactive session).
- [x] 2026-05-19 — `[D]` Switch program upgrade authorities to Squads — **DONE.** Transferred maps + registry (single-key → vault) via CLI, and oracle (deployed → vault). All three now require a 2-of-3 proposal to upgrade. (We did this ahead of the audit; the audit will now review programs already under multisig governance — strictly better.)
- [ ] `[D]` Audit kickoff (formal engagement, NDA) — **prep DONE 2026-05-20**, now blocked only on **firm selection (your call — Neodyme primary)**. Ready-to-send package: `docs/architecture/M05-audit-scope.md` (formal scope), `M05-audit-package-manifest.md` (hash-verified handoff bundle), `M05-audit-outreach.md` (filled-in Neodyme + Halborn messages). Open decisions before sending: repo visibility (public vs NDA), target date, Areta subsidy application, budget ceiling.
- [ ] `[O]` Audit response triage (when findings come in) — **BLOCKED:** audit complete.

### Done bucket

- [x] 2026-05-09 — Identified the Anchor deploy blocker (block-buffer 0.12 / Rust 1.85 / ephemeral-rollups-sdk transitive dep)
- [x] 2026-05-13 — Repaired in-page Phantom direct sign (v1.0.47/99) — builder confidence pre-mainnet
- [x] 2026-05-14 — Solana Playground confirmed NOT a fallback for oracle (anchor 0.29 + no receiver-sdk) — *later overturned, see oracle deploy below*
- [x] 2026-05-19 — Squads V4 multisig created on devnet (2-of-3, vault `fK1J2TLk2qLy3cjtiSYDSuCnWuxezphBcdqNGZEpVsp`)
- [x] 2026-05-19 — Upgrade authority for maps + registry + oracle all transferred to the multisig
- [x] 2026-05-19 — chartrunner_oracle deployed (`4vfZVDfD…`) + seed bug fixed + upgraded via a real 2-of-3 Squads cycle
- [x] 2026-05-19 — Security audit shipped (`SECURITY_AUDIT_2026-05-19.md`): keypair → 1Password, Anthropic + Telegram token rotation, no-OCR rule
- [x] 2026-05-20 — Pre-audit cleanup #1: **oracle program-ID mismatch fixed.** `Anchor.toml` (devnet+localnet) + `oracle/src/lib.rs` `declare_id!` rotated from the stale placeholder `7FJjBq98…` to the deployed `4vfZVDfD…` (matches `lib.playground.rs`). Source/config only — no redeploy. Zero placeholder occurrences remain repo-wide.
- [x] 2026-05-20 — Pre-audit cleanup #2: **IDLs committed for all three live programs** under `anchor/idl/` (registry copied from build output; maps + oracle hand-authored from source, schema-matched + field-checked). `target/` is gitignored, so `anchor/idl/` is the committed home. Unblocks oracle client integration + gives the auditor an IDL per program. Regenerate via `anchor build` once the toolchain unblocks.
- [x] 2026-05-20 — **Audit handoff package drafted** (`M05-audit-scope.md` + `M05-audit-package-manifest.md` + `M05-audit-outreach.md`): formal scope, SHA-256-verified source/IDL bundle manifest, and ready-to-send Neodyme + Halborn messages. Audit booking now gated only on firm selection + a few send decisions.
- [x] 2026-05-20 — **Self-audit reconciled to source** (`M05-self-audit.md §Reconciliation`). Every *exploitable* finding is already fixed in source (oracle owner-check + staleness clamps v0.9.13; registry `init`/`unsigned_abs`/`buyer!=seller`/`max_price` v0.9.8). Open items are structural/confirm work for the firm (oracle mirror-layout diff, dead fee path, dangling-listing). **Key risk surfaced: DEPLOY PARITY** — the deployed binaries can't be hash-verified against the fixed source until the Rust-1.85 wall unblocks the rebuild; the oracle owner-check fix in particular is almost certainly **not live on devnet** yet (postdates the last upgrade). Plan: audit the source now; rebuild + multisig-upgrade registry + oracle when the toolchain unblocks, then run the `solana program show` / dump-hash parity check. **[DONE 2026-05-26 → `docs/architecture/M05-deploy-parity.md` — parity table + rebuild/re-upgrade runbook delivered. Result: maps is LIVE-MATCHES-SOURCE (nothing to ship); registry + oracle are SOURCE-AHEAD (deployed bytecode predates the v0.9.8 / v0.9.13 fixes), oracle the priority. NEXT = the rebuild + 2-of-3 re-upgrade itself, which is Julian-hands (local `anchor build` + biometric 1P signing; sandbox has no Tailnet route), to be done BEFORE the audit kicks off.]**

## State (updated by evaluators)

- **Progress: 3 of 4 completion conditions DONE (~75%)** — multisig-wraps-authorities ✅ + oracle-deployed ✅ + **match-deployed ✅ (05-20, the Anchor wall fell)**. Remaining: **independent third-party audit — the single open condition** (Neodyme primary, hash-verified handoff package ready, gated only on your firm-selection + send decisions). M0.5 stays 🟢 ACTIVE until the audit lands.
- The multisig is fully exercised end-to-end (propose → 2 approvals → execute), not just created. Reusable scripts in `_security/squads/`. All self-audit findings closed across 3 hardening upgrades (oracle v0.9.13 forgeable-price owner-check, registry v0.9.8 init front-run + max_price slippage, match v0.9.10 host-only finish).
- Blockers active: **0** — the Anchor/ERS/Rust-1.85 wall is gone (platform-tools v1.52). The `cr-anchor-blocker-watch` sensor has been **disabled** (confirmed `enabled:false` in the scheduler as of 2026-05-25 — settled).
- **Deploy-parity (`M05-deploy-parity.md`):** **registry RESOLVED 2026-05-27** — rebuilt locally + 2-of-3 re-upgraded + byte-verified (tx `3XHRv5j…`, deployed == source), carrying the v0.9.8 fixes + name-register + resale + oracle-cite. **Oracle still SOURCE-AHEAD** (Item 4 SDK / owner-check rebuild skipped this session) — now the sole deploy-parity item, Julian-hands, ideally before the auditor looks.
- Last reconciled: 2026-05-26 (evening evaluator: no M0.5 change — Ready-bucket fully drained, audit still gated on Julian's firm-selection. Separately static-verified the registry Name-Register upgrade as SHIP-READY — `registry-name-register-verify-2026-05-26.md` — since that program is multisig-governed + feeds the audit scope). Earlier 2026-05-26 (morning: deploy-parity runbook DONE; anchor-blocker-watch disabled). Prior: 2026-05-24 (match-deployed sync), 2026-05-20.

## Notes

- Anchor deploy blocker: full context in memory `project_chartrunner_anchor_deploys.md`. Keypairs saved at `~/.chartrunner-keypairs/`.
- Three possible unblock paths: (a) Anza platform-tools w/ Rust 1.85+, (b) ERS release without block-buffer 0.12, (c) vendored block-buffer fork via `[patch.crates-io]`. Daily watch tracks all three.
- M0.5 unblocks M5 (MagicBlock ER for PvP) and M10 (mainnet deploy needs audit-clean programs).

### Ecosystem scan 2026-05-14
- **Squads** $18M strategic round (Solana Ventures + Coinbase Ventures + Haun + L1D) closed — infra remains healthy to depend on. Deploy economics unchanged: 0.1 SOL one-time + ~0.0028 SOL rent/fund. Source: `docs/SOLANA-ECOSYSTEM-DAILY.md#2026-05-14`.
- **Pyth** `pyth-solana-receiver-sdk` confirmed compatible with Anchor 0.28.0 / 0.29.0 / 0.30.1 / 0.31.1 (April 2026 upgrade). Worth noting whenever the Rust-1.85 / ERS unblock lands — Pyth side is already on the latest Anchor.
