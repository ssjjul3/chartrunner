# M2.6 — Avatar identity · hotkey execution USP

**Status:** 🟢 ACTIVE PRODUCT FOCUS (identity surface mostly shipped; USP/onboarding polish next)
**Theme:** Avatar import is the emotional hook and hotkey `4` is the execution hook. The first-minute demo should read: **import avatar → avatar becomes runner/cursor/profile identity → hotkey 4 initiates trades**. Wallet-held NFTs already become the player's avatar via the **Magic Eden public API** (not Helius DAS), and wallet-bound runner names render across the main profile surfaces. Remaining scope is now demo clarity and reliability: make import impossible to miss, verify the avatar image propagates to every surface, and tighten hotkey-4 trade activation.

> **Reconciled 2026-05-20** against committed code (see `MILESTONE_AUDIT.md §2`). This milestone had drifted badly — the doc read QUEUED·1/13 while the avatar + name features were live. Statuses below now reflect the shipped reality.

> **Update 2026-05-26** ([CONSOLIDATED_STATUS_2026-05-26.md](../../CONSOLIDATED_STATUS_2026-05-26.md)): the dedicated **`claim_name`/`release_name`** instructions + owner-less global-uniqueness PDA `[b"name", name_lower]` are now **WRITTEN** (registry source + `cr-registry-program.ts` builder + first-connect "🪪 Claim on-chain" modal + `crMarket.checkHandle` availability check) — this **closes gap #2 (global name uniqueness)** below. **LIVE on devnet 2026-05-27** — deployed in the batched 2-of-3 registry re-upgrade (exec tx `3XHRv5j…`, byte-verified); the 🪪 claim button now works on-chain. Implemented with lowercase-validated `name_lower` rather than `keccak256(name)` from `M26-name-register.md` — simpler, same uniqueness guarantee.

> **Updated 2026-05-30:** product focus moved away from terminal/runroom complexity and back to the USP. M2.5 made the SDK portable; M2.6 must make the product memorable. The next slice should prioritize avatar import and hotkeys over additional terminal variants: show the avatar in the profile, in-game runner, desktop cursor/avatar, and hotkey-4 trade activation flow before any economic/terminal surface.

## Completion condition

- [x] **DAS provider decision for avatar import** — **resolved for now:** picker uses Magic Eden's free no-auth API. Re-open Helius DAS only if Magic Eden becomes insufficient for cNFT support, collection badges/gating, or launch reliability.
- [x] **NFT picker filters to wallet-held NFTs** — v1.0.23. Live wallet holdings via Magic Eden `/v2/wallets/{owner}/tokens`, 5-min cache. Guest fallback is the procedural Invader/Snake sprite + an honest "connect a wallet" empty state (the curated-SVG fallback was retired).
- [x] **`drawPlayer` renders chosen NFT as in-game sprite** — v1.0.119/120. `_drawLoadoutAvatar` image-source branch + unified pipeline across every surface. ⚠ verify against the active avatar-display bug (`CONSOLIDATED_STATUS_2026-05-20.md §4`).
- [x] **Name Register PDA → global uniqueness** — dedicated `claim_name` / `release_name`, owner-less `[b"name", name_lower]` uniqueness PDA, devnet live 2026-05-27.
- [x] **First-connect name claim flow** — v1.0.122 + 2026-05-27 registry upgrade. Pick name → availability check → claim local or on-chain, wallet-bound, reverts to `anon_runner` with no wallet.
- [x] **Names display wherever `crLpName` is read** — v1.0.122. `crLpName`, `crLpBbText`, `osProfileName`, `crProfilePlayerName`. (Leaderboard/tournament surfaces land with M8.)
- [ ] **First-minute USP flow** — import avatar first, then immediately teach/trigger hotkey `4` as the trade initiator. Pass condition: a new player can understand "that is me" and "4 starts the trade" without opening Terminal/Runroom.
- [ ] **Avatar propagation QA** — selected avatar updates profile preview, lite profile portrait, in-game runner, desktop player avatar, phone cursor, and hotkey/execution surfaces after one selection.

## Imminent-solvables

### Ready bucket

- [x] `[D]` **Helius-vs-Magic-Eden decision** — `docs/architecture/M26-helius-das.md`. DAS endpoint coverage + what it buys over ME. **Decision: keep Magic Eden now; switch only on a real trigger (cNFT support, collection badges/gating, or launch-reliability).**
- [x] `[D]` **Helius pricing commit** — `docs/architecture/M26-helius-pricing.md`. Free $0 / Dev $49 / Business $499 / Pro $999 (milestone's "$99" was wrong). **Avatar-only workload fits Free indefinitely; don't provision paid for M2.6 alone.**
- [x] `[D]` **On-chain name uniqueness design** — `docs/architecture/M26-name-register.md`. Recommendation was Option B (uniqueness-index PDA + `claim_name`); implementation shipped with lowercase `name_lower` PDA seeds instead of hashed seeds.

### Reference docs (documenting what shipped) — written 2026-05-20

- [x] `[O]` **Picker audit** — `docs/architecture/M26-picker-audit.md`. Live Magic Eden picker + the single swap point (`_fetchWalletNfts`) for a future DAS migration.
- [x] `[O]` **drawPlayer image-source branch spec** — `docs/architecture/M26-drawplayer-image.md`. Documents the shipped `_drawLoadoutAvatar` NFT branch; specs the 2 parity gaps (upside-down red tint lost; hologram absent for both sprites).

### Blocked / open

- [x] `[D]` `claim_name` Anchor instruction added to `chartrunner_registry` — deployed devnet 2026-05-27 with global name uniqueness.
- [ ] `[D]` Helius client adapter + DAS-backed picker — **CONDITIONAL:** only if the decision is "switch from ME."
- [~] `[O]` Avatar-display bug **DIAGNOSED 2026-05-27** (`docs/architecture/M26-avatar-bug-diagnosis-2026-05-27.md`, 5.3 KB). Static debug ruled the loader provably correct (`img.src` assignment + boot restore both reached); top-2 root causes: **#1** `_nftApplyAvatar` round-trips through `localStorage` so a blocked/sandboxed/cleared store starves `_nftPreloadImage` → no request even after picking (the fit for "no network request + 527 turns unfixable in-file"), **#2** asymmetric `img-src` CSP at deploy edge. Paste-ready in-file diff shipped (direct preload + in-memory `_crNftSelected` fallback + one `console.debug` instrument); fixes #1 and decides #1-vs-#2 in a single reload. **Open:** Julian applies the diff + deploys + verifies in live build → then flip to `[x]`.
- [ ] `[D]` First-minute USP pass — replace terminal/runroom-first messaging with avatar import + hotkey-4 execution path.
- [ ] `[O]` Hotkey map regression — verify `1/2/3/4` remains learnable, with `4` consistently described as trade initiation / blue laser execution rather than runroom.

### Done bucket

- [x] 2026-05-04 — v0.9.95 offline NFT picker (top 20 Solana collections, curated SVGs) — *later superseded by the live picker.*
- [x] 2026-05-?? — **v1.0.23 live NFT picker** (real wallet holdings via Magic Eden public API; 5-min cache; honest empty/error states).
- [x] **v1.0.119/120 in-game NFT avatar sprite** (`_drawLoadoutAvatar` image branch; unified pipeline across small tile, big preview, in-game runner; two-pass CORS loader; tainted-canvas fallback).
- [x] **v1.0.121 tabs override NFT** (Invader/Snake default; picking a tab clears the NFT pick).
- [x] **v1.0.122 wallet-bound name claim** (local + on-chain via `ENTITY_TOKENPROFILE`; reverts to `anon_runner` with no wallet; renders on all `crLpName` surfaces).
- [x] 2026-05-30 — USP reframed: avatar import + hotkey `4` execution is the next visible product spine; terminal/runroom work is secondary.

## State

- Progress: identity foundation mostly done (wallet NFT picker, in-game sprite path, name claim + display, global uniqueness). Original "1/13" reflected the offline-picker-only snapshot and was stale.
- Real remaining work: (1) first-minute USP/onboarding pass, (2) avatar propagation + display bug closure, (3) optional Helius-vs-ME decision if Magic Eden becomes insufficient.
- Status: 🟢 active product focus until avatar import + hotkey-4 execution is unmistakable in the demo.

## Notes

- The implementation diverged from the original plan in one lasting way: **Magic Eden's free public API replaced the Helius DAS adapter**. The earlier generic `save_entity` shortcut has since been corrected by the dedicated `claim_name` / `release_name` upgrade.
- The `claim_name` upgrade is live on devnet; keep it in the M0.5 independent audit scope.
- Sets up M3 (NFT-gated bots), M4 (NFT-as-asset listings), M8 (named players in tournament brackets).
- Guests with no wallet must keep seeing a working avatar — that is now the procedural Invader/Snake sprite, not the retired curated-SVG grid.

### Ecosystem scan 2026-05-14
- **Helius** pricing structure unchanged: credit-based plans + LaserStream/Enhanced WS data tiers fixed at $500/mo (5 TB) → $6,000/mo (100 TB); overage at 2 credits per 0.1 MB. DAS API surface stable — the M26-helius-pricing.md memo can commit on current numbers. Source: `docs/SOLANA-ECOSYSTEM-DAILY.md#2026-05-14`.
