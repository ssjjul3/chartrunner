deployed: 2026-05-11T14:00:00Z · v1.0.50 (Desktop avatar no longer leaks into the game when starting a run — hideSplash() had two bugs: (1) the cleanup gate checked `body.classList.contains('crInvaderCursor')`, but that class is only set during a drag (mousedown→mouseup), so a calmly-sitting avatar never tripped the gate and toggleOsInvaderCursor never fired; switched to the `window.crOsInvaderActive` flag the OS-invader IIFE owns. (2) The fallback canvas sweep checked z-index '99999', but v1.0.46 bumped the avatar canvas to '1000001'; switched the selector to `canvas[data-cr-os-invader]` which is stable across z-index bumps. Now: activate Player → start run → hideSplash → toggle off + sweep stragglers + flip flag false. Avatar gone the moment the chart appears.)
