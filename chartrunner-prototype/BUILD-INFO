deployed: 2026-05-11T20:00:00Z · v1.0.56 (Zerion CLI autonomous agent wrapper for Zerion Frontier Track — $5k USDC. New sdk-m1-scaffold/sdk/agents/zerion-cli.js exports ZerionAgent base class that wraps any detector function as an autonomous agent. Six pre-built agents ship today wrapping the in-game SDK's pattern detectors: ccvAgent / sfpAgent / hnsAgent / barrAgent / faAgent / oiAgent — same detector code that powers the in-game Coach. agent.run(ctx) → { decisions: [{ action, mint, size, reason, confidence, setup }] }. Optional auto-execute via agent.execute(decision, broker) routes through the v1.0.53 Jupiter / v1.0.54 Jito broker chassis. fetchPortfolio(walletPubkey) wraps Zerion's /v1/wallets/<pubkey>/positions endpoint for portfolio-aware sizing (defaults to 1% of SOL balance, clamped [0.01, 1.0]). Registry API: registerAgent / listAgents / getAgent. window.crAgents exposes everything for in-game UI + DevTools testing.)
