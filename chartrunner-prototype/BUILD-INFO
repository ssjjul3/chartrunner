deployed: 2026-05-11T23:00:00Z · v1.0.59 (GoldRush by Covalent for GoldRush Frontier Track — $3k USDC. New window.crGoldRush IIFE wraps Covalent's API across 4 subsystems: tokenHolders (Phoenix Live whale-ghosts), walletBalances (Zerion fallback), walletTxs (M4 marketplace widget pre-wire), nftMetadata (Helius DAS fallback). 60s cache per (chain, endpoint, params) key + in-flight dedupe + API key from window.crGoldRushKey or localStorage.cr_goldrush_key_v1. Second IIFE shims Phoenix Live's whale-ghosts overlay: every 90s pulls real top-10 holders for the active asset's mint via GoldRush tokenHolders_v2; falls back to Phoenix's legacy stub if GoldRush returns nothing. Same API shape across 200+ chains so post-mainnet cross-chain expansion is wire-up not rebuild.)
