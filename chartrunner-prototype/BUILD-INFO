deployed: 2026-05-11T15:00:00Z · v1.0.51 (SNS Identity Track integration — first post-Frontier sidetrack build. New crSnsIdentity IIFE resolves the connected wallet's primary .sol domain via Bonfida's public SNS API (3 endpoint fallback chain: sns-sdk-proxy workers → /v2/user/* → legacy /fav-domain). 10-min cache per pubkey + in-flight dedupe means one HTTP per wallet connect, zero HTTP per 500-ms billboard tick. Result stashed on window._crSnsName; _refreshLightProfile prefers this over the legacy profileName input over 'anon_runner'. Cross-network: lookup hits mainnet SNS registry even though ChartRunner runs on devnet (same pubkey, both networks). Hooked into crWallet.on() so connect/disconnect triggers exactly one fetch. Sets up M2.6 "Claim runner subdomain" flow for later — once we own chartrunner.sol parent, the read-only path becomes a write path via Bonfida's subdomain ix.)
