deployed: 2026-05-11T12:00:00Z · v1.0.48 (Lite-profile dot-matrix billboard rotator + drop "Trader" subtitle — replaces the L/R equipment chips around the avatar with a single green LED dot-matrix billboard at the top of the panel that cross-fades through three readouts every 2.5 s: name (anon_runner) → asset · TF (BTC · 15m) → mode (Time is Money / Speedrun / Monster Mode / CH.X · campaign goal). Billboard styled to look like the cabin LED signs in reference: black bg, repeating-radial-gradient pixel grid, mint-on-black bold mono with multi-layer text-shadow glow. Theme overrides locked to mint-on-black across all four themes so the sign reads as hardware regardless of OS chrome. Equipment columns (#crLpEqL/#crLpEqR) hidden but kept in DOM for back-compat with the existing chip-distribution code in _refreshLightProfile. Rotator IIFE pauses when panel is closed (MutationObserver on .on class) to save CPU.)
