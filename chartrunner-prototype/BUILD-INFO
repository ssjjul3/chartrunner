deployed: 2026-05-11T02:00:00Z · v1.0.38 (Hardened theme-window image upload after v1.0.37 still didn't fire — added stopPropagation on the pick button so the modal-backdrop close handler can never see it; the file input is now replaced with a freshly-cloned node after each pick (Chrome quirk: change won't re-fire for the same filename even if .value resets); drag-and-drop onto the preview tile as a backup path; visible toast checkpoints + console.log at every step (pick clicked / picker opened / file change fired / FileReader loaded / bg applied) so any future failure pinpoints itself; modal z-index bumped 99500 → 99700 to clear the v1.0.20 NFT-picker overlay; preview tile gets dashed-border + cursor:copy + hover state so drag-drop is visually inviting.)
