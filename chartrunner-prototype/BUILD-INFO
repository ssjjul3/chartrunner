deployed: 2026-05-11T13:00:00Z · v1.0.49 (Hide every menubar clock — per user spec "in headbar delete time and day in desktop and in game". Single CSS rule hides #crClock (desktop Apple menubar), #crBarClock (in-game OS bar), #osClock (older splash taskbar), #topClock (in-game topbar) with !important so theme overrides can't unhide. The JS tickers (osUpdateClock, tickClock, tickBar) keep firing harmlessly — they just write text into invisible nodes, no perf impact, easy to restore by removing the one CSS line.)
