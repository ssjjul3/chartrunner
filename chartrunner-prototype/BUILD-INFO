deployed: 2026-05-12T07:00:00Z · v1.0.67 (Four bundled fixes:

1. Coach widget typing fix. crCoach.subscribe was calling renderWidgets() on every Coach state tick — destroying the entire widget DOM including the input element, wiping the user's in-progress typing. Replaced with new _softRefreshChatWidgets() that walks .cr-widget [data-chat-log] elements, rebuilds ONLY the log container's innerHTML (last 50 messages), leaves the input + send button + outer chrome untouched. Preserves scroll-at-bottom state. Same pattern as v1.0.66's _softRefreshTokenWidgets.

2. Token Terminal archive — HYPE, PUMP, MOG, FART marked archived:true in TOK_LIST. After v1.0.63's no-synth-fake policy these rendered "—" forever because none had a Binance pair AND none had a verified Birdeye mint. _tokRenderList now filters them out. Restoration is one flag flip + a mint, no code change.

3. USDT + USDC also archived from Token Terminal — they're settle-asset infrastructure (price floats $1 ±0.01%, low-signal as watchlist rows). Still in TOK_BIRDEYE_MINT so the broker chassis + setQuoteAsset work; just hidden from the visible token list.

4. USDT + USDC cards added to lite-profile Wallet tab. Two-column row inside one card showing real balances (USDT in Tether green #26a17b, USDC in Coinbase blue #2775ca, "—" when unknown). New IIFE polls window.crGoldRush.walletBalances every 3 min for the connected wallet, scans tokens for the USDT + USDC mints, stashes on window._crLpStableBal which _refreshLightProfile reads synchronously on its 500-ms tick. Refetches on wallet change via crWallet.on.)
