deployed: 2026-05-11T19:00:00Z · v1.0.55 (Tether USDT first-class settle asset for Tether Frontier Track — $10k USDT. Added MINT_USDT constant + QUOTE_ASSETS map + setQuoteAsset/getQuoteAsset/listQuoteAssets API to jupiter.js with localStorage persistence (cr_quote_asset_v1). decimalsFor(mint) helper resolves SOL=9 / USDC=6 / USDT=6 so the quote+fill math is mint-aware not USDC-hardcoded. jito.js imports the shared constants + helpers — wrapper inherits USDT support transparently. brokers/index.js re-exports setQuoteAsset + MINT_* for single entry point. Fill records on both Jupiter + Jito now include quoteAsset:'USDC'|'USDT' so journal shows which stablecoin settled. In-game Token Terminal: USDT + USDC added to TOK_LIST as 'sol'-chain rows + TOK_BIRDEYE_MINT mapped so both stablecoins surface with real Birdeye data (mc / holders / 24h volume / liquidity).)
