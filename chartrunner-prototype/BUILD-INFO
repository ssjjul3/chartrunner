deployed: 2026-05-12T06:00:00Z · v1.0.66 (Pinned token widget flicker fixed. Root cause: v1.0.63's mass-fetch (22 staggered token fetches per list render) interacted badly with _tokScheduleRerender's throttle-on-leading-edge 200ms window. Each fetch resolution fired renderWidgets/renderChartWidgets → layer.innerHTML='' destroyed every widget canvas → _crPaintTokSparks repainted within 1s → dead frame in between = visible flicker. Two-part fix: (1) _tokScheduleRerender → proper trailing-edge debounce, window 200→800ms so 22 fetches collapse to ONE settle at the end. (2) Replaced destructive renderWidgets/renderChartWidgets calls with new _softRefreshTokenWidgets() that walks every canvas[data-w-tok-spark], climbs to .cr-widget, updates price + 24h% + live-dot text in-place via data-w-tok-* attributes baked into the widget body. No DOM destruction = no canvas destroy = no flicker. Sparklines stay alive and get repainted on the existing 1s _crPaintTokSparks tick from fresh _tokLiveCache data.)
