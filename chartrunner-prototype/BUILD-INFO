deployed: 2026-05-11T11:00:00Z · v1.0.47 (Direct in-page Phantom signing for chartrunner_maps save — no more /solana-connect/ bounce. New crMapsTx IIFE owns signAndSendSave(name, hashBytes): computes Anchor discriminator sha256("global:save_map")[:8], Borsh-encodes [disc + u32 LE name length + name bytes + 32-byte content_hash], derives map PDA from ["map", owner, name] under DbzEqKfgCBqneR6Yuc17yEPc1fbVeqTeGy721f1n3UvH, builds Transaction with [map(W) · owner(WS) · systemProgram(!W !S)], gets latest blockhash from api.devnet.solana.com, calls phantom.signAndSendTransaction(tx), confirms via conn.confirmTransaction. anchorByName tries direct path first; falls back to the legacy /solana-connect/?action=save-map redirect bounce if @solana/web3.js failed to load, Phantom isn't injected, or signing throws. @solana/web3.js loaded async via <script> tag in <head> (~270 KB cached). Map delete still uses the bounce since the deployed chartrunner_maps program lacks a delete instruction; v0.9.64's crRegistry.deleteFromChain('map', name) writes to the registry program which doesn't actually own the map PDA — flagged for post-Frontier program upgrade.)
