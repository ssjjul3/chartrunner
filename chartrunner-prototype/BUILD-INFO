deployed: 2026-05-11T17:00:00Z · v1.0.53 (Jupiter broker adapter for Jupiter Frontier Track — $3k jupUSD. New sdk-m1-scaffold/sdk/brokers/jupiter.js implements the BrokerAdapter contract. Submit flow: GET /v6/quote → POST /v6/swap → decode b64 versioned tx → provider.signAndSendTransaction → confirmTransaction. Returns fill record with txSig + routePlan + in/out amounts so the game journal shows WHERE the swap actually filled across Solana DEXes. Default pair SOL↔USDC; mintIn/mintOut overridable. No SDK dependency, no API key required for moderate rate. Registered in brokers/index.js — switch via setBroker('jupiter'). First LIVE on-chain broker in the registry; phoenix still pending @ellipsis-labs/phoenix-sdk publish.)
