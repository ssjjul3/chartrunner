deployed: 2026-05-11T18:00:00Z · v1.0.54 (Jito broker wrapper for Jito Frontier Track — $2k USDC. New sdk-m1-scaffold/sdk/brokers/jito.js composes on top of Jupiter (imports quote + buildSwapTx, no duplication). Submit flow: Jupiter quote → Jupiter swap tx → build sibling tip tx with matching recentBlockhash → provider.signAllTransactions([swap, tip]) → POST /api/v1/bundles to mainnet.block-engine.jito.wtf with sendBundle method → poll getBundleStatuses until finalized (30s timeout, 1.5s interval). Returns fill record with bundleId + swapSig + tipSig + tipLamports + routePlan. Default tip 10000 lamports (~$0.0017), override via order.tipLamports. 8 Jito tip accounts embedded, selected per-submission via Math.random. Graceful fallback to plain RPC send when block engine errors or signAllTransactions unsupported. Registered in brokers/index.js as 5th driver alongside mock / binance-paper / phoenix / jupiter.)
