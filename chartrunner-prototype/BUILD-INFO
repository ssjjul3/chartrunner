deployed: 2026-05-11T07:00:00Z · v1.0.43 (Background upload, third try — previous attempts (v1.0.36 label/for, v1.0.37 button+file.click, v1.0.38 button+stopProp+nodeReplace) all relied on a programmatic file.click() that the user reported never opened the picker. v1.0.43 swaps the hidden input + button for a NATIVE VISIBLE file input that the user clicks directly — browser opens the picker, no programmatic call in the path. Visible status line inside the modal shows every step (▶ Reading X.png · ✓ Applied 1234 KB cached · ✗ Read failed: ...) so failures are user-visible without DevTools. _applyBg now writes background / background-size / background-position / background-repeat / backdrop-filter inline on #splash with !important via setProperty('important'), bypassing all CSS specificity battles with theme overrides. Drag-drop onto preview tile still works as the second path.)
