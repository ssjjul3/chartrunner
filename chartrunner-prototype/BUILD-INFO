deployed: 2026-05-12T00:00:00Z · v1.0.60 (Terminal pane schemas + 6 live-data wirings — Layer A + Layer B. LAYER A: 24 new _CR_PANE_SCHEMAS entries cover every Solana + CEX + Phoenix-HL pane so the "+ New Terminal Widget" picker shows real per-pane settings (asset selector, refresh slider, row count, sort order, tracked-wallets textarea, etc) instead of the generic fallback. New 'select' field type added to BOTH renderers (picker cfg + per-widget settings popup). LAYER B: 6 panes wired to live data: solPrice (Birdeye), solHolders (GoldRush tokenHolders), solSmart (GoldRush walletTxs), cexExchanges (CoinGecko /exchanges), cexFunding (Binance /fapi/v1/premiumIndex sorted by abs rate), cexListings (CoinGecko /coins/list/new). New IIFE crTerminalPanes auto-tags Solana+CEX panes with data-pane-id via header-text mapping (stable across reloads), runs a 15s master tick with per-pane refreshSec self-throttle pulled from cr_pane_defaults_v1 localStorage. Live panes flash 'LIVE' hd-tag, mock panes stay tagged with their original badge.)
