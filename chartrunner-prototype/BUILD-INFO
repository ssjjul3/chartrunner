deployed: 2026-05-12T01:00:00Z · v1.0.61 (Token Terminal: Birdeye-backed snapshot + GoldRush top-10 enricher + Settings panel. _tokSnapshot refactored to prefer live.mc / live.liquidity / live.holders / live.supply from the Birdeye-populated _tokLiveCache over synth seeded values; FDV derived from supply × price when both real. 7d change computed from cached klines (first vs last close) when Binance returned them, synth fallback otherwise. New mcLive / liqLive / holdersLive / fdvLive / top10Live / ch7dLive flags on the snapshot let the profile renderer paint a green ● live badge next to each field that came from a real source. New crTokTop10 IIFE polls window.crGoldRush.tokenHolders for the active token every 90s, computes sum of top-10 share, stashes on _tokLiveCache so _tokSnapshot reads it. New ⚙ Settings button in the Token Terminal window header opens a slide-in panel: default timeframe, profile refresh interval, Birdeye API key, GoldRush API key, "Hide synth-only tokens" toggle. Settings saved to cr_tokterm_settings_v1 localStorage; API keys forwarded to crBirdeye.setApiKey + crGoldRush.setApiKey on save; hideSynth filter applied in _tokRenderList.)
