deployed: 2026-05-12T05:00:00Z · v1.0.65 (Double-click any pinned widget → its config window. Single document-level delegated dblclick handler (capture phase) catches dblclick anywhere inside .cr-widget, walks to find the widget's data-id, looks it up in widgets.items (desktop) or chartWidgets.items (chart layer), calls the existing _crTermPaneQuickConfig(w, onSave) popup. onSave persists via _wSave / _cwSave and re-renders the right layer so field changes (tokenId for solHolders, tracked wallets for solSmart, etc) take effect immediately. Suppressed on inner interactive bits — title bar drag handle [data-drag], close button [data-close], resize grip .wResize, all native form controls (button/input/select/textarea/a) — they keep their own click semantics. Visual affordance: cursor:zoom-in on .wBody hover so the gesture is discoverable. "+ New Terminal Widget" picker stays for SPAWNING new widgets; this is the per-widget EDIT path.)
