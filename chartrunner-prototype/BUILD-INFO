deployed: 2026-05-12T04:00:00Z · v1.0.64 (Birdeye OHLCV kline fallback — Phase 1 of the mixed-source candle plan. crBirdeye IIFE adds fetchOhlcv(tokenId, tf) wrapping /defi/ohlcv?address=&type=&time_from=&time_to= with 60s cache + in-flight dedupe. TF map: 15m→15m / 1h→1H / 4h→4H / 1D→1D / 1W→1W. Returns { klines:[{t,o,h,l,c,v}], closes:[c] } in the exact shape _tokLiveCache + _tokDrawSparkline already expect. _tokFetchLive fires Birdeye OHLCV in parallel with Binance for sol-chain tokens; merge logic: if Birdeye-only path (no Binance pair) → use Birdeye OHLCV for klines+closes (was: empty arrays = synth sparkline); if Binance returned thin klines (< 7 candles) but Birdeye OHLCV is fuller → swap to Birdeye OHLCV. dataSource tag now reads 'binance', 'birdeye/birdeye-ohlcv', or 'binance+birdeye/binance' so origin is queryable. Solana memecoin sparklines render real cross-DEX OHLC (aggregating Phoenix + Raydium + Orca + Meteora + Lifinity) instead of falling through to synth.)
