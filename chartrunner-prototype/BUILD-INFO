deployed: 2026-05-11T01:00:00Z · v1.0.37 (Fix theme switch + bg upload that landed broken in v1.0.36 — applyTheme is scoped inside the ChartRunnerOSExt IIFE so it wasn't reachable from the theme-window IIFE; theme card clicks silently no-op'd. Replaced with direct splash[data-theme] flip — the existing MutationObserver in TVChrome auto-mirrors that to body[data-os-theme] so all per-theme CSS overrides activate immediately. File upload was using <label for=...> with display:none input which Arc/Chrome silently ignored; replaced with a real <button> that programmatically calls file.click(), input visually-offscreen instead of display:none. Bonus: bumped html-body-prefixed selector specificity so per-theme .splash overrides don't trump the uploaded background image.)
