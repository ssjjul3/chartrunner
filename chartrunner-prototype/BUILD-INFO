deployed: 2026-05-11T22:00:00Z · v1.0.58 (LI.FI cross-chain funding widget for LI.FI Superteam Germany Frontier Track — $2.5k USDC. New "+ Fund" button on the lite-profile Wallet tab's $CRDS card opens a Jumper Exchange iframe modal (LI.FI's own frontend, full LI.FI routing under the hood) pre-configured for ChartRunner: toChain=Solana (1151111081099710), toToken=USDC mint, toAddress=connected wallet auto-filled from crWallet.get(). Modal lazy-builds on first open; iframe src cleared to about:blank on close so LI.FI session doesn't keep polling chain APIs in background. Backdrop-click + Escape close handlers; sandbox attrs include popups-to-escape-sandbox so Phantom wallet popups render when triggered from inside the iframe. window.crLifiFunding exposes open/close. Reinforces the Visa Germany submission's stablecoin-payment-infrastructure framing — same widget covers two tracks.)
