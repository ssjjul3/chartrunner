deployed: 2026-05-11T05:00:00Z · v1.0.41 (App icons toggle on second click — matches RUN-tube. Added osToggleWindowMulti(prog) helper that opens the window if closed, closes + defocuses + clears the OS status if already open. Re-wired desktop .os-icon clicks (capture-phase + stopImmediatePropagation to override the v0.7b open-only listener), .dockBtn clicks, and the custom-app icon injection at line 25849. Programmatic opens (tutorial steps, post-action navigation like Create Token → Tokenterm) keep using osOpenWindowMulti directly so they don't accidentally close a window the caller wanted open. osCloseAllWindows still works the same way.)
