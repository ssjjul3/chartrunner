// Shared types used across the core SDK. These match the shapes the existing
// ChartRunner_Prototype.html already uses, so M1 extraction is a graft, not
// a port.
export {};
//# sourceMappingURL=types.js.map