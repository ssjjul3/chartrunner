# Vite + React build artifacts
node_modules/
dist/
.vite/
*.local
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# v0.9.8 — TypeScript incremental-build cache. Per-machine, never commit.
tsconfig.tsbuildinfo
*.tsbuildinfo

# Pitch decks live at repo root, not inside solana-connect/
ChartRunner_*.pdf
