{
  "name": "chartrunner-ownership",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ownership worker — the only writer of verified ownership rows; checks Solana payments, subscriptions and wallet signatures before anything counts as an asset.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node ../../scripts/check_ownership_worker.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
