{
  "name": "chartrunner-account",
  "version": "1.0.0",
  "private": true,
  "description": "Account deletion worker — verifies the caller's Supabase JWT and deletes their own account + data via the service-role key.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
