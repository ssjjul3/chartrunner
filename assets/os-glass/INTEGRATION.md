# SUPERSEDED 2026-07-17

This proposed a **new** OS glass theme — wrong scope. ChartRunnerOS already has a
`liquid` (Liquid Glass) theme. The corrected, in-place reinforcement lives in:

- `liquid-theme-reinforcement.css` — patch keyed to the existing `[data-theme="liquid"]` surfaces
- `REINFORCEMENT.md` — the spec

No new theme; reinforce the current ones.
