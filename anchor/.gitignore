# Anchor / Cargo build artifacts
.anchor
target/
**/*.rs.bk
node_modules/
.DS_Store
test-ledger/
.idea/

# Per-developer wallets — never commit. Devs use ~/.config/solana/id.json
# (referenced from Anchor.toml) which Anchor never touches inside this dir.
*.json.tmp
deploy.log
